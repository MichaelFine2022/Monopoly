{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build Monopoly",
            "command": "g++ -g ${workspaceFolder}/*.cpp -o ${workspaceFolder}/monopoly.exe",
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}